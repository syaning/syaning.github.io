---
title: 781. Rabbits in Forest
tags: Medium Array HashTable Math Greedy
---

[View on LeetCode](https://leetcode.com/problems/rabbits-in-forest/)

**Approach:** Count answers; for each a, rabbits come in groups of a+1 via ceil(count/(a+1))*(a+1).

**Complexity:** O(n) time, O(n) space

::: code-group

```js
/**
 * @param {number[]} answers
 * @return {number}
 */
var numRabbits = function(answers) {
  const map = answers.reduce((ret, answer) => {
    ret[answer] = (ret[answer] || 0) + 1
    return ret
  }, {})

  return Object.entries(map).reduce((ret, [answer, count]) => {
    answer = Number(answer)
    ret += Math.ceil(count / (answer + 1)) * (answer + 1)
    return ret
  }, 0)
}
```

```py
import math


class Solution:

    def numRabbits(self, answers):
        """
        :type answers: List[int]
        :rtype: int
        """
        counts = {}
        for answer in answers:
            counts[answer] = counts.get(answer, 0) + 1

        total = 0
        for answer, count in counts.items():
            total += math.ceil(count / (answer + 1)) * (answer + 1)

        return total
```

:::
